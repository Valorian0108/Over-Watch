{
  "buildCommand": "cd vercel-deploy && npm install && npm run build",
  "outputDirectory": "vercel-deploy/dist",
  "installCommand": "cd vercel-deploy && npm install",
  "framework": null,
  "functions": {
    "api/**": {
      "runtime": "nodejs18.x"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}